# ===========================================
# 🐳 Odoo Development Environment Template
# ===========================================
# Copia este archivo a .env y personaliza las variables

# 🗄️ PostgreSQL Database Configuration
POSTGRES_USER=odoo
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD_$(openssl rand -hex 16)
POSTGRES_DB=postgres

# 🔐 Odoo Configuration
ODOO_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD_$(openssl rand -hex 12)
ODOO_HOST=localhost
HOST=postgres
USER=odoo
PASSWORD=CHANGE_ME_SECURE_PASSWORD_$(openssl rand -hex 16)

# 🌐 pgAdmin Configuration (OPCIONAL - Solo para desarrollo)
PGADMIN_DEFAULT_EMAIL=admin@yourcompany.com
PGADMIN_DEFAULT_PASSWORD=CHANGE_ME_PGADMIN_PASSWORD_$(openssl rand -hex 12)
PGADMIN_LISTEN_PORT=80

# 🚀 Development Settings (Optional)
# Uncomment and modify as needed

# ODOO_EXTRA_ARGS=--dev=all
# ODOO_LOG_LEVEL=debug
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432

# � SEGURIDAD - IMPORTANTE:
# ========================================
# 1. NUNCA uses estos passwords en producción
# 2. Genera passwords únicos para cada instalación
# 3. POSTGRES_PASSWORD y PASSWORD deben coincidir
# 4. Para generar passwords seguros usa: openssl rand -base64 32
# 5. En producción, desactiva pgAdmin y cierra puerto PostgreSQL

# 📝 INSTRUCCIONES DE SETUP:
# ========================================
# 1. Copia este archivo: cp .env.example .env
# 2. Edita .env y cambia TODAS las passwords
# 3. Reemplaza $(openssl rand -hex 16) con passwords reales
# 4. NO commitees el archivo .env a git